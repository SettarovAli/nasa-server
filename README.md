# NASA app express server

## Clone the project:

```shell
git clone https://github.com/SettarovAli/nasa-server.git
```

## Install dependencies:

```shell
cd nasa-server
npm install
```

## Run locally:

```shell
npm run dev
```
